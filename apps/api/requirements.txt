fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.4
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# Phase 2 — LangGraph workflow
# langgraph is pinned exactly to avoid pip scanning 70+ release versions.
# All langchain-* are minimum constraints; pip resolves compatible versions.
langgraph==0.2.76
langchain-openai>=0.2.14
langchain-anthropic>=0.2.4
langchain-google-genai>=2.1.4

# Testing dependencies
httpx>=0.27.0
pytest>=8.2.0
pytest-asyncio>=0.23.0
